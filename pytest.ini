# Kořen repa musí být na sys.path, aby šel importovat balíček `ollamaproxy`
# i při spuštění konzolovým skriptem `pytest` (CI). `python -m pytest` to
# dělá sám, `pytest` ne.
[pytest]
pythonpath = .
testpaths = tests
